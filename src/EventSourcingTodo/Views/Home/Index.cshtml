@model EventSourcingTodo.Models.IndexViewModel
@{
    ViewData["Title"] = "Home Page";
}
<div class="row">
    <div class="col-sm-10 col-sm-offset-1 col-md-8 col-md-offset-2">
        <form asp-controller="Home" asp-action="Index" method="post" role="form">
            <div class="row">
                <div class="col-sm-9">
                    <div class="form-group">
                        <label asp-for="PostModel.Description" class="sr-only"></label>
                        <input asp-for="PostModel.Description" class="form-control" placeholder="Add a new To-do" />
                        <span asp-validation-for="PostModel.Description"></span>
                    </div>
                </div>
                <div class="col-sm-3">
                    <button type="submit" class="btn btn-success btn-block">Add To-do</button>
                </div>
            </div>
        </form>
    </div>
</div>
<br />
<div class="row">
    <div class="col-sm-10 col-sm-offset-1 col-md-8 col-md-offset-2">
        <ul class="list-group">
            @foreach (var todo in Model.TodoList)
            {
                <li class="list-group-item">@todo.Description</li>
            }
        </ul>
    </div>
</div>